import { BaseService } from "./BaseService";


export class CartService extends BaseService {
    constructor(self) {
        super(self)
    }

    get(attrs) {
        super.get(attrs)
        // 
    }
    addItem(attrs) {
        
    }
}